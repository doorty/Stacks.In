<%= form_for(@stack) do |f| %>
  <% if @stack.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@stack.errors.count, "error") %> prohibited this stack from being saved:</h2>

      <ul>
      <% @stack.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field span-18 last">
    <%= f.label :title %>
    <%= f.text_field :title, :class => "title span-18 last" %>
  </div>
  <div class="actions span-18 last">
    <%= f.submit "Create Stack", :class => "button orange large span-18 last" %>
  </div>
<% end %>
